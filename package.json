{
  "name": "esp32-androidtv-release-tooling",
  "private": true,
  "description": "semantic-release config only — the actual project is ESP-IDF/C, see main/",
  "scripts": {
    "release": "semantic-release"
  },
  "devDependencies": {
    "semantic-release": "^24.2.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1"
  }
}
